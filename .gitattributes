# Git LFS configuration for large files
# Track large binary files and datasets with Git LFS

# Demo dataset files (Illumina data) - Optional download
# Users can fetch with: git lfs fetch --include="datasets/demo/**" && git lfs checkout datasets/demo/
datasets/demo/** filter=lfs diff=lfs merge=lfs -text

