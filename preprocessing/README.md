# Preprocessing Tools

Sample validation and pairing file generation utilities.

## Directory Structure

```
preprocessing/
├── preprocessing_config.yaml  # Configuration for sample validation
└── preprocessing_run.sh       # Sample name validation and pairing script
```

## Usage

Run `./preprocessing_run.sh` to validate sample names across input files and generate the sample pairing file required by the pipeline.
